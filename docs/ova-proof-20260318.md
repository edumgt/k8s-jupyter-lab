# OVA Validation Proof (2026-03-18)

Environment:
- Host: Windows + VirtualBox
- Guest VM: `k8s-data-platform` (Ubuntu 24.04.4 LTS)
- SSH: `ubuntu@127.0.0.1:2620`

Console screenshot:

![OVA VM login prompt](screenshots/ova-proof-vm-console.png)

## Command Evidence

Executed inside guest after login:

```bash
id
kubectl version --client
kubeadm version
docker --version
node --version
npm --version
dpkg -s code-server | grep '^Version:'
sudo systemctl is-active docker containerd kubelet code-server
kubectl get nodes -o wide
kubectl get pods -n data-platform-dev -o wide
kubectl get svc -n data-platform-dev
```

Observed output snapshot:

```text
uid=1000(ubuntu) gid=1000(ubuntu) groups=... ,112(docker)
Client Version: v1.34.5
kubeadm version: ... GitVersion:"v1.34.5" ...
Docker version 28.2.2
node v22.22.1
npm 10.9.4
code-server Version: 4.111.0
systemd: docker/containerd/kubelet/code-server = active

node:
k8s-data-platform   Ready   control-plane   v1.34.5

pods(data-platform-dev):
airflow   Running
backend   Running
frontend  Running
jupyter   Running
mongodb   Running
redis     Running
gitlab    Running (0/1 warm-up)
nexus     ImagePullBackOff

services(data-platform-dev):
frontend  NodePort 30080
backend   NodePort 30081
jupyter   NodePort 30088
gitlab    NodePort 30089 (SSH 30224)
airflow   NodePort 30090
nexus     NodePort 30091
```

## Notes

- `apply_k8s.sh` CRLF issue (`pipefail\r`) was fixed by normalizing shell line endings during provisioning.
- Dev overlay now includes local hostPath PV resources for single-node VM PVC binding.
- `nexus` remains `ImagePullBackOff` because `docker.io/edumgt/platform-nexus3:3.90.1-alpine` could not be pulled in this run.
